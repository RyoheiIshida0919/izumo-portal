---
import BaseLayout from '../layouts/BaseLayout.astro';
import Breadcrumb from '../components/common/Breadcrumb.astro';
import Button from '../components/common/Button.astro';
import { CONTACT_FORM_URL, SITE_NAME } from '../lib/constants';

const pageTitle = 'お問い合わせ';
const pageDescription = `${SITE_NAME}へのお問い合わせページです。`;
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8 py-8">
    <Breadcrumb items={[{ label: pageTitle }]} />

    <article class="bg-white rounded-xl shadow-sm p-6 lg:p-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-8">{pageTitle}</h1>

      <div class="prose prose-gray max-w-none mb-8">
        <p>
          {SITE_NAME}へのお問い合わせは、以下のフォームよりお願いいたします。
        </p>
        <p>
          なお、個別の施設・サービスに関するお問い合わせは、各施設へ直接ご連絡ください。
        </p>
      </div>

      <div class="bg-gray-50 rounded-lg p-6 text-center">
        <h2 class="text-lg font-bold text-gray-900 mb-4">お問い合わせフォーム</h2>
        <p class="text-gray-600 mb-6">
          下記ボタンからGoogleフォームに移動します
        </p>
        <Button href={CONTACT_FORM_URL} size="lg">
          お問い合わせフォームを開く
        </Button>
      </div>

      <div class="mt-8 p-4 bg-primary-50 rounded-lg">
        <h3 class="font-bold text-gray-900 mb-2">掲載をご希望の方へ</h3>
        <p class="text-sm text-gray-600">
          お店やサービスの掲載をご希望の方は、<a href="/media" class="text-primary-600 hover:underline">掲載希望ページ</a>をご覧ください。
        </p>
      </div>
    </article>
  </div>
</BaseLayout>
